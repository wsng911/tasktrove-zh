"use client"

/**
 * Catch-all route that handles all client-side routing
 * This replaces individual route files with a single dynamic handler
 */
export default function CatchAllPage() {
  // Content is handled by MainLayoutWrapper based on the current pathname
  return null
}
