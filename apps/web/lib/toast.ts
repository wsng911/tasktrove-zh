// Centralized toast exports so surfaces can swap implementations (e.g. Capacitor on mobile).
export { toast, Toaster } from "sonner"
export type { ToasterProps, ExternalToast, ToastT } from "sonner"
